check if user uploaded correct files

send info to download page about 
	1. students that did not attend the exam
	2. students that scored higher than 100 (and corrected to 100)
	3. students that can not be found in template (possible erasmus students)
	4. add a message for reminding visual check for users

style htmls

check if user downloads the correct files with session.userid
error handling for wrong files/file types/files that do not belong to user
	-check the length of the files if all files are correct but in different order
	-use try except blocks in pandas functions to see if the files are correct files

change the form to wtforms